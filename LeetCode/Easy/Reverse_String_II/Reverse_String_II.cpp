#include <iostream>
#include <string>

std::string reverseStr(std::string s, int k);

int main() {
  std::string s;
  int k;
  std::cin >> s;
  std::cout << reverseStr(s, k) << std::endl;
  return 0;
}

std::string reverseStr(std::string s, int k) {}