#include <iostream>
#include <vector>

std::vector<int> constructRectangle(int area);

int main() {
  std::vector<int> result;
  for (int i : result) {
    std::cout << i << " ";
  }
  return 0;
}

std::vector<int> constructRectangle(int area) {
  std::vector<int> result;
  return result;
}